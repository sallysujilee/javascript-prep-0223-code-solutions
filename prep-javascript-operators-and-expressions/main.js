const number1 = 2;
const number2 = 3;
const product = number1 * number2;
console.log('value of product:', product)
console.log('type of product:', product);

const charge = 1;
const payment = 5;
const amountRemaining = payment - charge;
console.log('value of amountRemaining:', amountRemaining);
console.log('type of amountRemaining:', amountRemaining);

const tests = 60;
const assignments = 100;
const final = 80;
const grade = (tests + assignments + final)/3;
console.log('value of grade:', grade);
console.log('type of grade:', grade);

const firstName = "Sally";
const lastName = "Lee";
const fullName = firstName + lastName;
console.log('value of fullName:', fullName);
console.log('type of fullName:', fullName);

const pH = 5;
const isAcidic = pH < 7;
console.log('value of isAcidic:', pH);
console.log('type of isAcidic:', isAcidic);

const numberOfSoldiers = 300;
const isSparta = numberOfSoldiers === 300;
console.log('value of isSparta:', numberOfSoldiers);
console.log('type of isSparta:', isSparta);

let nickname = fullName;
nickname += " is the GOAT";
console.log('value of nickname:', nickname);
console.log('type of nickname:', nickname);
